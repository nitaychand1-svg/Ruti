# Инструкция по развертыванию Trading System в Pydroid 3

## Шаг 1: Подготовка

1. Установите **Pydroid 3** из Google Play
2. Откройте Pydroid 3

## Шаг 2: Скопируйте файлы проекта

1. Скопируйте файл `setup_trading_project.py` на ваше Android устройство
2. Разместите его в папке хранилища файлов (например, `/storage/emulated/0/Download/`)

## Шаг 3: Запуск установки

1. Откройте Pydroid 3
2. Нажмите на кнопку "Open" и выберите `setup_trading_project.py`
3. Нажмите на кнопку "Run" (▶)
4. Скрипт создаст полную структуру проекта в `/storage/emulated/0/trading_system/`

## Шаг 4: Установка зависимостей (опционально)

```bash
pip install fastapi uvicorn python-json-logger networkx pyyaml
```

⚠️ **ВНИМАНИЕ**: Не все пакеты могут работать в Pydroid 3. Для серьезных - используйте Linux сервер или Docker.

## Шаг 5: Проверка установки

После завершения работы скрипта можно будет увидеть следующую структуру:

```
/storage/emulated/0/trading_system/
├── .sops.yaml
├── Dockerfile
├── requirements.txt
├── README.md
├── .github/
│   └── workflows/
│       └── build_and_push.yaml
├── app/
│   ├── main.py
│   ├── config.yaml
│   ├── modules/
│   │   ├── __init__.py
│   │   ├── api_routes.py
│   │   ├── cognitive_middleware.py
│   │   ├── data_sources.py
│   │   ├── llm_wrapper.py
│   │   ├── logging_config.py
│   │   ├── observability.py
│   │   ├── rl_agent.py
│   │   └── taskgraph.py
│   ├── tasks/
│   │   ├── __init__.py
│   │   └── debate_tasks.py
│   └── tests/
│       ├── __init__.py
│       ├── test_debate_tasks.py
│       └── test_taskgraph.py
└── k8s/
    ├── deployment_security.yaml
    ├── network_policy.yaml
    ├── vault-policy.hcl
    └── vault-rotation-job.yaml
```

## Для чего это нужно

- **Изучение кода**: Исследование Pydroid 3 для просмотра и редактирования кода
- **Запуск скриптов**: Локальное тестирование отдельных модулей
- **Разработка**: Быстрая разработка в дороге на мобильном устройстве

## Известные ограничения

### Не все пакеты работают
Если скрипт не может создать файлы, проверьте права доступа к `/storage/emulated/0/`

### Сторонние пакеты
Некоторые пакеты (OpenTelemetry, Prometheus) могут быть несовместимы с Pydroid 3. Эти компоненты для production сред.

### Запуск FastAPI
Полноценный запуск FastAPI сервера в Pydroid 3 может быть проблемным. Для production используйте полный Linux сервер или Docker.

## Альтернативная база установки

Если базовый путь `/storage/emulated/0/` недоступен, отредактируйте переменную `BASE_DIR` в начале скрипта:

```python
BASE_DIR = "/storage/emulated/0/trading_system"  # Измените на другой путь
```

## Использование на Linux/Mac

Если вы хотите развернуть проект на обычной машине:

```bash
# Клонируйте или создайте проект
cd /workspace
python3 setup_trading_system_project.py

# Установите зависимости
pip install -r requirements.txt

# Запустите тесты
pytest app/tests/ -v

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## API примеры

После запуска сервера вы можете использовать следующие endpoints:

### Проверка здоровья
```bash
curl http://localhost:8000/health
# Ответ: {"status": "ok"}
```

### Метрики Prometheus
```bash
curl http://localhost:8000/metrics
```

### Торговое решение
```bash
curl http://localhost:8000/debate/AAPL
# Ответ:
# {
#   "decision": {
#     "action": 0.85,
#     "reason": "LLM analysis: Positive outlook..."
#   },
#   "corr_id": "123e4567-e89b-12d3-a456-426614174000"
# }
```

### Режим отладки
```bash
curl "http://localhost:8000/debate/AAPL?debug=true"
# Показывает полные результаты всех задач в графе
```

## Docker Development

Для разработки через Docker:

```bash
# Сборка образа
docker build -t trading-system:dev .

# Запуск с монтированием кода для live reload
docker run -p 8000:8000 -v $(pwd)/app:/app/app trading-system:dev
```

## Тестирование

```bash
# Все тесты
pytest app/tests/ -v

# С покрытием кода
pytest app/tests/ --cov=app --cov-report=html

# Только один файл
pytest app/tests/test_taskgraph.py -v
```

## Структура TaskGraph

Граф задач выполняется в следующем порядке:

```
fetch_news (получение новостей)
    ↓
llm_analysis (анализ через LLM)
    ↓
cognitive (обработка результатов)
    ↓
rl_decision (торговое решение)
```

Каждый узел может иметь зависимости, и система автоматически выполняет их в правильном порядке.

## Добавление новых задач

Пример добавления новой задачи в граф:

```python
# В app/tasks/debate_tasks.py

async def sentiment_analysis_task(context, deps):
    news = deps['fetch_news']['news']
    # Ваша логика анализа
    return {"sentiment": "positive"}

def create_debate_graph(ticker: str):
    tg = TaskGraph()
    tg.add(Node("fetch_news", fetch_news_task))
    tg.add(Node("sentiment", sentiment_analysis_task, depends_on=["fetch_news"]))
    tg.add(Node("llm_analysis", llm_analysis_task, depends_on=["fetch_news", "sentiment"]))
    # ... остальные узлы
```

## Troubleshooting

### Ошибка: Config load error

**Проблема:** Не найден файл конфигурации

**Решение:** Убедитесь, что `app/config.yaml` существует и содержит:

```yaml
vault:
  address: "https://vault.example.com"
  role_id: "your-role-id"
  secret_id: "your-secret-id"

kubernetes:
  namespace: "trading-system"

otel:
  endpoint: "http://otel-collector:4317"
```

### Ошибка: ModuleNotFoundError

**Проблема:** Не установлены зависимости

**Решение:**
```bash
pip install -r requirements.txt
```

### Ошибка: Permission denied

**Проблема:** Нет прав на создание файлов

**Решение:** 
- На Android: Проверьте разрешения для Pydroid 3
- На Linux: Используйте путь, где у вас есть права записи

---

**Успехов с разработкой! 🚀**

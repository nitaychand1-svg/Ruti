# Инструкция по установке Trading System в Pydroid 3

## Шаг 1: Установка

1. Установите **Pydroid 3** из Google Play
2. Откройте Pydroid 3

## Шаг 2: Подготовка скрипта

1. Скачайте файл `setup_trading_system_project.py` на ваш Android устройство
2. Сохраните его в доступную папку (например, `/storage/emulated/0/Download/`)

## Шаг 3: Запуск скрипта

1. Откройте Pydroid 3
2. Нажмите на кнопку "Открыть" (Open) и выберите `setup_trading_system_project.py`
3. Нажмите на кнопку "Запустить" (Run) 
4. После выполнения скрипт создаст структуру проекта в `/storage/emulated/0/trading_system/`

## Шаг 4: Установка зависимостей (опционально)

```bash
pip install fastapi uvicorn python-json-logger networkx pyyaml
```

**Примечание**: Не все пакеты могут быть доступны в Pydroid 3. Для полной функциональности - используйте полноценный Linux сервер или Docker.

## Шаг 5: Структура проекта

После выполнения скрипта вы получите следующую структуру проекта:

```
/storage/emulated/0/trading_system/
├── .sops.yaml
├── Dockerfile
├── requirements.txt
├── README.md
├── .github/
│   └── workflows/
│       └── build_and_push.yaml
├── app/
│   ├── main.py
│   ├── config.yaml
│   ├── compat.py
│   ├── modules/
│   │   ├── __init__.py
│   │   ├── api_routes.py
│   │   ├── cognitive_middleware.py
│   │   ├── data_sources.py
│   │   ├── llm_wrapper.py
│   │   ├── logging_config.py
│   │   ├── observability.py
│   │   ├── rl_agent.py
│   │   └── taskgraph.py
│   ├── tasks/
│   │   ├── __init__.py
│   │   └── debate_tasks.py
│   └── tests/
│       ├── __init__.py
│       ├── test_debate_tasks.py
│       └── test_taskgraph.py
└── k8s/
    ├── deployment_security.yaml
    ├── network_policy.yaml
    ├── vault-policy.hcl
    └── vault-rotation-job.yaml
```

## Возможные проблемы

- **Права доступа**: Если скрипт не может создать файлы, проверьте права доступа к `/storage/emulated/0/`
- **Зависимости**: Некоторые пакеты (OpenTelemetry, Prometheus) могут быть недоступны в Pydroid 3. Для полной функциональности используйте полноценный Linux сервер или Docker.
- **Запуск FastAPI**: Полноценный запуск FastAPI сервера в Pydroid 3 может быть затруднен. Для production рекомендуется использовать полноценный Linux сервер или Docker.

## Настройка базовой директории

Если базовый путь `/storage/emulated/0/` недоступен, измените переменную `BASE_DIR` в скрипте установки:

```python
BASE_DIR = "/storage/emulated/0/trading_system"  # Измените на ваш путь
```

---

**Удачи в разработке! 🚀**

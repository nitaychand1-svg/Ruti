# Trading System

Production-ready trading system с автоматическими торговыми решениями на основе AI.

## 🚀 Основные возможности

- **FastAPI Backend** - современный асинхронный веб-фреймворк
- **OpenTelemetry Tracing** - распределенная трассировка запросов
- **Prometheus Metrics** - сбор метрик производительности
- **TaskGraph Execution** - граф задач с параллельным выполнением
- **LLM Integration** - интеграция с языковыми моделями для анализа
- **RL Agent (PPO)** - агент обучения с подкреплением для принятия торговых решений
- **Kubernetes Ready** - готовность к деплою в K8s
- **Vault Secrets** - управление секретами через HashiCorp Vault

## 📋 Структура проекта

```
/workspace/
├── app/
│   ├── main.py                  # Главный FastAPI приложение
│   ├── config.yaml              # Конфигурация
│   ├── compat.py                # Патчи совместимости для Python 3.13
│   ├── modules/
│   │   ├── api_routes.py        # API маршруты
│   │   ├── cognitive_middleware.py  # Промежуточная обработка
│   │   ├── data_sources.py      # Источники данных (новости)
│   │   ├── llm_wrapper.py       # Обертка для LLM
│   │   ├── logging_config.py    # Настройка логирования (JSON)
│   │   ├── observability.py     # OpenTelemetry setup
│   │   ├── rl_agent.py          # RL агент (PPO)
│   │   └── taskgraph.py         # Граф задач
│   ├── tasks/
│   │   └── debate_tasks.py      # Граф задач для торговых решений
│   └── tests/
│       ├── test_debate_tasks.py # Тесты для торговых задач
│       └── test_taskgraph.py    # Тесты для графа задач
├── k8s/
│   ├── deployment_security.yaml # Kubernetes deployment
│   ├── network_policy.yaml      # Сетевые политики
│   ├── vault-policy.hcl         # Vault политики
│   └── vault-rotation-job.yaml  # Ротация секретов
├── .github/workflows/
│   └── build_and_push.yaml      # CI/CD пайплайн
├── Dockerfile                    # Docker образ
├── requirements.txt              # Python зависимости
└── .sops.yaml                    # SOPS конфигурация

```

## 🛠️ Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск тестов

```bash
pytest app/tests/ -v
```

### 3. Запуск приложения

```bash
# Development режим
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Production режим
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### 4. Docker

```bash
# Сборка образа
docker build -t trading-system:latest .

# Запуск контейнера
docker run -p 8000:8000 trading-system:latest
```

### 5. Kubernetes

```bash
# Применить манифесты
kubectl apply -f k8s/deployment_security.yaml
kubectl apply -f k8s/network_policy.yaml
```

## 📡 API Endpoints

### Health & Metrics

- `GET /health` - проверка здоровья сервиса
  ```json
  {"status": "ok"}
  ```

- `GET /metrics` - Prometheus метрики
  ```
  # HELP http_requests_total Total HTTP requests
  # TYPE http_requests_total counter
  http_requests_total{method="GET",endpoint="/health",status="200"} 42
  ```

### Trading

- `GET /debate/{ticker}` - получить торговое решение для тикера
  
  **Параметры:**
  - `ticker` (path) - символ акции (например, AAPL, TSLA)
  - `debug` (query, optional) - режим отладки с полными результатами
  
  **Пример запроса:**
  ```bash
  curl http://localhost:8000/debate/AAPL
  ```
  
  **Ответ:**
  ```json
  {
    "decision": {
      "action": 0.85,
      "reason": "LLM analysis: Positive outlook..."
    },
    "corr_id": "123e4567-e89b-12d3-a456-426614174000"
  }
  ```

- `GET /example` - пример endpoint с трассировкой
  ```json
  {
    "ok": true,
    "corr_id": "123e4567-e89b-12d3-a456-426614174000"
  }
  ```

## 🔐 Безопасность

- **Non-root контейнер** - приложение работает от пользователя с UID 1000
- **Network policies** - ограничение исходящего трафика
- **Vault integration** - безопасное хранение секретов
- **SOPS encryption** - шифрование конфигурации
- **Cosign signing** - подпись Docker образов

## 🧪 Тестирование

Проект использует:
- **pytest** - для unit тестов
- **pytest-asyncio** - для асинхронных тестов
- **hypothesis** - для property-based тестирования

```bash
# Запуск всех тестов
pytest app/tests/ -v

# Запуск с покрытием
pytest app/tests/ --cov=app --cov-report=html
```

## 📊 Мониторинг и Observability

### OpenTelemetry

Автоматическая трассировка всех HTTP запросов:
- Correlation IDs для отслеживания запросов
- Span'ы для каждого шага обработки
- Экспорт в OTLP collector

### Prometheus Metrics

- `http_requests_total` - счетчик запросов
- `http_request_duration_seconds` - гистограмма латентности

### Structured Logging

JSON логи с полями:
- `timestamp` - время события
- `level` - уровень логирования
- `name` - имя логгера
- `message` - сообщение
- `corr_id` - correlation ID

## 🔄 Граф задач (TaskGraph)

Система использует DAG (направленный ациклический граф) для выполнения задач:

```python
fetch_news → llm_analysis → cognitive → rl_decision
```

1. **fetch_news** - получение новостей по тикеру
2. **llm_analysis** - анализ новостей через LLM
3. **cognitive** - обработка результатов анализа
4. **rl_decision** - принятие торгового решения через RL агента

## 🤖 RL Agent

Агент использует алгоритм PPO (Proximal Policy Optimization):
- Вход: обработанный анализ новостей и контекст
- Выход: торговое действие (-1 до 1, где -1 = продать, 1 = купить)

## 📝 Конфигурация

Конфигурация хранится в `app/config.yaml`:

```yaml
vault:
  address: "https://vault.example.com"
  role_id: "your-role-id"
  secret_id: "your-secret-id"

kubernetes:
  namespace: "trading-system"

otel:
  endpoint: "http://otel-collector:4317"
```

## 🚨 Troubleshooting

### Проблема: Config load error

**Решение:** Проверьте, что `app/config.yaml` существует и содержит валидные параметры Vault.

### Проблема: Зависимости не устанавливаются в Pydroid 3

**Решение:** Некоторые пакеты (OpenTelemetry, Prometheus) могут не работать в Pydroid 3. Используйте Linux или Docker для production.

### Проблема: Permission denied для /storage/emulated/0

**Решение:** Это Android путь. На Linux используйте `/workspace` или другой доступный путь.

## 📱 Использование в Pydroid 3

См. `PYDROID_INSTRUCTIONS.md` для инструкций по настройке проекта в Pydroid 3 на Android устройстве.

## 🔧 Разработка

### Добавление новых задач в TaskGraph

1. Создайте async функцию в `app/tasks/`
2. Добавьте Node в граф с зависимостями
3. Добавьте тесты в `app/tests/`

### Добавление новых API endpoints

1. Добавьте handler в `app/modules/api_routes.py`
2. Используйте correlation_id из `request.state.corr_id`
3. Добавьте логирование с `extra={"corr_id": corr_id}`

## 📄 Лицензия

MIT License

## 👥 Contributing

Pull requests приветствуются! Для значительных изменений, пожалуйста, откройте issue для обсуждения.

---

**Создано для production-ready AI торговых систем** 🚀📈
